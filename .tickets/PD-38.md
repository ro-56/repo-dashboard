---
created: "2026-07-31T02:01:11Z"
dependencies:
    - PD-36
    - PD-35
id: PD-38
links: []
mode: afk
priority: 2
status: open
tags: []
title: 'Reskin: roster grid / repo card / project section geometry to v2'
type: task
updated: "2026-07-31T02:02:01Z"
---

## What to build

Port `RosterColumnLabels.svelte`, `RosterRow.svelte`, `RepoCard.svelte`, and `ProjectSection.svelte`'s hardcoded pixel dimensions to the v2 proportions, including updating the `--roster-cols` token to the v2 track widths. Re-verify the responsive floor (ADR-0009): shed order stays grants-text → distribution bar → project meta → chip labels → transition from-level, and the grant-source column never sheds, even though the v2 columns are wider overall — adjust breakpoint thresholds if the new widths require it.

## Acceptance criteria

- [ ] `--roster-cols` matches v2's track widths; `RosterColumnLabels` and `RosterRow` both reflect it with no remaining hardcoded literal
- [ ] `RepoCard` and `ProjectSection`'s rendered spacing/heights match the v2 mockup
- [ ] At the 900px floor, the shed order is unchanged from today and the grant-source column is still never dropped
- [ ] `pnpm check` passes
- [ ] Verified via `pnpm tauri dev`, including a resize test down to 900px