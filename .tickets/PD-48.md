---
created: "2026-07-31T13:17:52Z"
dependencies:
    - PD-44
id: PD-48
links:
    - PD-43
    - PD-44
    - PD-45
    - PD-46
    - PD-47
    - PD-49
    - PD-50
mode: afk
priority: 2
status: in_progress
tags: []
title: 'FilterBar: restyle segmented control to match reference'
type: task
updated: "2026-07-31T17:17:36Z"
---

## What to build

Per PD-43, restyle the "All access" / "Changes only" tabs in `FilterBar.svelte` to match the reference: a soft grey track (`--track`) holding a white "pressed" pill (`--track-active`) with a subtle shadow (`--shadow-segment`) on the active tab. Replace the current bordered button group (hard dividers between tabs, solid inverted black/white active state) entirely.

No change to the tab-switching interaction/click-handling model — this is a styling-only change.

## Acceptance criteria

- [ ] Tab container renders as a grey track with 2px inner padding, no border
- [ ] Inactive tabs have no divider between them and no visible background
- [ ] The active tab renders as a white pill with a subtle shadow, not a solid dark background
- [ ] `pnpm check` passes
- [ ] Verified visually via `pnpm dev`/`pnpm tauri dev` against the reference, in both tab states