---
created: "2026-07-31T13:17:56Z"
dependencies:
    - PD-44
id: PD-49
links:
    - PD-43
    - PD-44
    - PD-45
    - PD-46
    - PD-47
    - PD-48
    - PD-50
mode: afk
priority: 2
status: open
tags: []
title: 'NoticeStrip: add structural badge pill'
type: task
updated: "2026-07-31T13:20:27Z"
---

## What to build

Per PD-43, add the solid "STRUCTURAL" badge pill `NoticeStrip.svelte` is missing (reference renders it as a filled pill before the prose text; current code renders it as plain letter-spaced text with no box). While touching this component: fix the strip's `border-bottom` color, which currently uses the generic `--rule` instead of the notice-specific `--notice-edge`; and remove the `inset 3px 0 0 var(--notice-edge)` left-gutter shadow, which isn't present in the reference.

## Acceptance criteria

- [ ] "Structural" label renders as a solid pill badge (background `--notice-badge`, text `--notice-badge-ink`) before the prose
- [ ] Strip's bottom border uses `--notice-edge`, not `--rule`
- [ ] The stray inset left-gutter shadow is removed
- [ ] `pnpm check` passes
- [ ] Verified visually via `pnpm dev`/`pnpm tauri dev` against the reference