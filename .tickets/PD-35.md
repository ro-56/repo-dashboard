---
created: "2026-07-31T02:00:55Z"
dependencies: []
id: PD-35
links: []
mode: afk
priority: 2
status: open
tags: []
title: 'Prefactor: single-source the roster grid width via a --roster-cols token'
type: task
updated: "2026-07-31T02:00:55Z"
---

## What to build

Add a `--roster-cols` custom property to `tokens.css` capturing the roster grid's column-width string, currently duplicated identically in `RosterColumnLabels.svelte` and `RosterRow.svelte`'s scoped `grid-template-columns` declarations. Update both components to reference `var(--roster-cols)` instead of the hardcoded literal. No visual change — this is pure de-duplication so a later geometry reskin only needs to touch the token value once instead of two components in lockstep.

## Acceptance criteria

- [ ] `--roster-cols` exists in `tokens.css` with today's exact value (`12px 26px 46px 70px 148px 132px 1fr`)
- [ ] `RosterColumnLabels.svelte` and `RosterRow.svelte` both reference `var(--roster-cols)` in their `grid-template-columns`, with no hardcoded literal remaining in either
- [ ] `pnpm check` passes
- [ ] Visual output is pixel-identical to before (verified via `pnpm tauri dev`)