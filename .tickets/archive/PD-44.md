---
created: "2026-07-31T13:17:29Z"
dependencies: []
id: PD-44
links:
    - PD-43
    - PD-45
    - PD-46
    - PD-47
    - PD-48
    - PD-49
    - PD-50
mode: afk
priority: 2
status: closed
tags: []
title: 'Prefactor: port design tokens for segmented control, distribution bar, meter, sigil, escalation tint, notice badge'
type: task
updated: "2026-07-31T13:26:38Z"
---

## What to build

Port the remaining design tokens `perm-diff.css` defines but `tokens.css` doesn't yet have, following the project's existing naming convention (not the reference's raw names where a convention already exists — e.g. `--state-added` not `--added-ink`). These tokens back the fixes in the rest of PD-43's breakdown; this ticket only adds the tokens, it does not consume them anywhere yet.

Tokens to add, with light-theme values sourced from `perm-diff.css`'s `:root` block (the authoritative finalized stylesheet — not the JS-prototype constants in the `.dc.html` mockups, which disagree with it on a few literal pixel values):

- `--track`, `--track-active`, `--shadow-segment` (filter-bar segmented control)
- `--r-row` (6px, segmented-control tab radius)
- `--segment-off`, `--seg` (9px, roster-row meter)
- `--bar-admin`, `--bar-write`, `--bar-read` (distribution bar)
- `--escalation-row` (subtle escalation row tint, distinct from the existing `--escalation-bg` badge tint)
- `--notice-badge`, `--notice-badge-ink` (notice strip badge pill)
- `--h-sigil` (18px), `--h-chip` (20px)

## Acceptance criteria

- [x] All twelve tokens above are added to `tokens.css`'s light-theme `:root` block with values matching `perm-diff.css`
- [x] No component styling changes in this ticket — tokens are added but unused
- [x] `pnpm check` passes
## Notes

Ported 14 tokens (segmented control, distribution bar, roster-row meter, sigil, escalation row tint, notice badge) into tokens.css's light-theme :root, matching perm-diff.css exactly. Unused, per ticket scope.
