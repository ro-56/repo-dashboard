---
created: "2026-07-29T01:53:23Z"
dependencies:
    - PD-10
id: PD-11
links: []
mode: afk
priority: 2
status: closed
tags: []
title: 'Roster dashboard: expand/collapse + full member roster'
type: task
updated: "2026-07-29T14:26:06Z"
---

## What to build

Extend the collapsed-overview dashboard (PD-10) with the expanded-card interaction: clicking a repo card expands it in place to show the full member roster (every Principal + access_type + permission on that repo) from the same Roster tree already being fetched — no new Tauri command. Still no diff markers rendered (the Run pair may differ, but this ticket only renders the roster rows themselves, not their diff status).

## Acceptance criteria

- [ ] Clicking a collapsed repo card expands it to list every Principal on that repo (Direct/Group/Member access_type, permission level, display label) from the already-fetched Roster tree.
- [ ] Multiple cards can be expanded simultaneously; collapsing one doesn't affect others.
- [ ] The all-collapsed state from PD-10 is unaffected/still reachable (collapse-all remains state `1b`).
- [ ] No additional Tauri command or Rust change — purely a frontend rendering change against the existing tree shape from PD-10.

## Out of Scope

- Diff markers in the expanded roster — next ticket.
- Escalation-highlighting toggle, changes-only filter — later tickets.
## Notes

Extended collapsed-overview dashboard with expand/collapse: clicking a repo card toggles a reactive SvelteSet of expanded keys, rendering the full Principal roster (label, access_type, permission) for that repo from the already-fetched Roster tree. Multiple cards expand independently; PD-10's all-collapsed state is unaffected. No new Tauri command or Rust change.
