---
created: "2026-08-02T15:53:26Z"
dependencies:
    - PD-80
id: PD-82
links:
    - PD-79
mode: afk
priority: 2
status: closed
tags: []
title: Localize chrome & drawer strings (HeadBar/FilterBar/EmptyState/SideDrawer panels)
type: task
updated: "2026-08-02T17:07:55Z"
---

## What to build

Extract every hardcoded English string in the surrounding chrome and side-drawer flows into the i18n catalog (en + pt-BR):

- `HeadBar.svelte` / `headBar.ts`: run/snapshot selector labels, delta chips
- `FilterBar.svelte`: filter controls
- `EmptyState.svelte`: empty-state messaging
- `SideDrawer.svelte`, `RunPanel.svelte`, `SnapshotsPanel.svelte`, `CredentialsPanel.svelte`, `ConfirmApplyDialog.svelte`: every drawer flow (Run now, view Snapshots, manage credentials, confirm Apply)

`AppearancePanel.svelte`'s own labels (e.g. "appearance", "Light"/"Dark") are included here too, since PD's infrastructure ticket only needed to prove the picker mechanism, not fully localize its own panel.

## Acceptance criteria

- [ ] No hardcoded English string literals remain in the components listed above
- [ ] Every key has both an English and a reviewed pt-BR translation
- [ ] Manually verified via `pnpm tauri dev`: switching Display language re-renders the head bar, filter bar, empty state, and every side-drawer panel/flow in the selected language with no leftover English strings
## Notes

Localized HeadBar/FilterBar/EmptyState and every side-drawer panel (Run/Snapshots/Credentials/ConfirmApplyDialog/Appearance) into en/pt-BR svelte-i18n catalogs; threaded Translate through headBar.ts/filterBar.ts/emptyState.ts.
