---
created: "2026-07-31T20:28:19Z"
dependencies: []
id: PD-56
links:
    - PD-55
    - PD-57
mode: afk
priority: 2
status: closed
tags: []
title: Fix side drawer chrome/spacing drift to match v2 reference
type: task
updated: "2026-07-31T20:40:21Z"
---

## What to build

Bring `SideDrawer` and its three child panels (`CredentialsPanel`, `RunPanel`, `SnapshotsPanel`) in line with `perm-diff.css`'s `.drawer`/`.drawer-head`/`.drawer-title`/`.close`/`.panel`/`.snapshot-row__actions` classes, per PD-55. Chrome and spacing only — no functional/behavioral changes to any panel.

`perm-diff.css` is the value source of truth, not the `.dc.html` mockup's inlined styles (which drift slightly from the stylesheet in places).

## Acceptance criteria

- [ ] Drawer title (`settings`) letter-spacing changed from 0.09em to 0.14em, matching every other 10px uppercase label in the app
- [ ] New `--shadow-drawer` token added to `tokens.css`'s light `:root` block (`-8px 0 20px rgba(20,24,28,.08)`) and applied as the open drawer's box-shadow
- [ ] Drawer background changed from `--surface-raised` to `--surface`
- [ ] Drawer z-index changed from 10 to 40
- [ ] Drawer close button and every button in `CredentialsPanel`/`RunPanel`/`SnapshotsPanel` use `--r-row` (6px) instead of `--radius` (2px) for corner radius
- [ ] Each panel's title-to-content gap changed from `--s-6` (12px) to `--s-5` (10px)
- [ ] `SnapshotsPanel`'s per-row Confirm/Cancel/Delete action-button gap changed from `--s-5` (10px) to `--s-3` (6px)
- [ ] `pnpm check` passes
- [ ] Verified via `pnpm tauri dev`: drawer visually matches `perm-diff.css` values in light mode; open drawer's shadow/z-index correctly layers above an open roster row's "⋯" context menu
## Notes

Resynced SideDrawer/CredentialsPanel/RunPanel/SnapshotsPanel chrome to perm-diff.css: drawer-title letter-spacing (0.09em->0.14em), new --shadow-drawer token applied to drawer box-shadow, background surface-raised->surface, z-index 10->40, button radius radius->r-row (also credentials input, matching perm-diff.css's .panel-field rule), panel title-to-content gap s-6->s-5, snapshot action-button gap s-5->s-3. pnpm check + vitest pass; verified visually via headless Chromium against the vite dev server with mocked Tauri invoke (chromium-cli/tauri dev GUI unavailable in this sandbox).
