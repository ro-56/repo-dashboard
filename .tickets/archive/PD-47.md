---
created: "2026-07-31T13:17:49Z"
dependencies:
    - PD-44
id: PD-47
links:
    - PD-43
    - PD-44
    - PD-45
    - PD-46
    - PD-48
    - PD-49
    - PD-50
mode: afk
priority: 2
status: closed
tags: []
title: 'CountBadges: convert delta counts to filled chips'
type: task
updated: "2026-07-31T14:49:55Z"
---

## What to build

Per PD-43, every delta count kind in `CountBadges.svelte` (added/removed/modified/escalation/no-change) should render as a filled color chip — matching the visual treatment the head bar's delta chips already use — instead of today's bare colored text (only the escalation kind currently gets a boxed treatment, and it uses the wrong height/radius tokens).

Reuse the existing `--state-added`/`-bg`, `--state-revoked`/`-bg`, `--state-changed`/`-bg`, `--escalation-ink`/`-bg` tokens already in `tokens.css` — no new tokens needed beyond `--h-chip` from PD-44.

## Acceptance criteria

- [ ] Added/removed/modified/escalation/no-change counts all render as filled chips (`--h-chip` height, `--r-chip` radius, mono weight 600)
- [ ] Chip colors match the equivalent head-bar delta chip's colors for each kind
- [ ] `pnpm check` passes
- [ ] Verified visually via `pnpm dev`/`pnpm tauri dev` against the reference, on both a repo card header and a project header
## Notes

Rewrote CountBadges.svelte so all five delta-count kinds (added/removed/modified/escalation/no-change) share a .chip base class using --h-chip/--r-chip, replacing the old bare-text .count-* styling and the escalation kind's hardcoded 15px/--radius sizing. Colors match HeadBar's equivalent chip--{tone} tokens; fixed no-change chip to use --ink-3 (not --ink-faint) to match HeadBar/reference precedent, caught in code review. Verified visually via a static tokens.css-driven preview screenshot at both repo-card and project-header scale.
