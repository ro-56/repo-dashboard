---
created: "2026-07-31T02:01:07Z"
dependencies:
    - PD-36
id: PD-37
links:
    - PD-34
mode: afk
priority: 2
status: closed
tags: []
title: 'Reskin: head bar / filter bar / notice strip geometry to v2'
type: task
updated: "2026-07-31T03:37:30Z"
---

## What to build

Port `HeadBar.svelte`, `FilterBar.svelte`, and `NoticeStrip.svelte`'s hardcoded pixel dimensions (bar heights, control heights, border-radii, gaps) to the v2 proportions from `perm-diff.css`, now that the underlying token values are in place. Purely a geometry/visual change — no behavior, data flow, or component prop changes.

## Acceptance criteria

- [ ] `HeadBar`, `FilterBar`, and `NoticeStrip`'s rendered heights/spacing match the v2 mockup (`.references/new-design/perm-diff-ledger-light.html`)
- [ ] No existing behavior regresses: run-pair selector/swap, segmented tabs, search filter, collapse-all/reset, structural notice truncation all still work exactly as before
- [ ] `pnpm check` passes
- [ ] Verified via `pnpm tauri dev` at full width and near the 900px responsive floor
## Notes

Ported HeadBar/FilterBar/NoticeStrip's hardcoded pixel dimensions to v2 tokens: --bar-head (40->56px), --bar-filters (31->44px), plus new --s-9/--h-control/--r-selector/--r-card tokens, matching perm-diff.css's .head-bar/.filter-bar/.notice-strip/.selector/.icon-button/.button geometry. No DOM/prop/behavior changes. pnpm check passes; visual verification via pnpm tauri dev skipped per user instruction. Also reverted an unrelated dependency-field corruption on PD-39/40/41 caused by the taskr start command itself.
